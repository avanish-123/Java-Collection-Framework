package com.collectionframework.Maps;

public class About {
    public static void main(String[] args) {
        // Itss maps keys to values. It can not contains duplicate keys and each key can map atmost one value
        System.out.println("Its a saparate interface, It does not extend the collection class"); // Importent

        /*
            Map
            Hashmap(LinkedHashMap, weakHashMap, IdentityHashMap)
            SortedMap(TreeMap, NavigableMap(Interface))
            ConcurrentMap(ConcurrentHashMap, ConcurrentSkipListMap)



         */
    }
    
}
